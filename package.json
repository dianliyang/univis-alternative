{
  "name": "univis-kiel-static-catalog",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "discover": "tsx scripts/discover.ts",
    "crawl": "tsx scripts/crawl.ts",
    "parse": "tsx scripts/parse.ts",
    "normalize": "tsx scripts/normalize.ts",
    "generate": "tsx scripts/generate.ts",
    "import:xml": "tsx scripts/import-xml.ts",
    "fetch:data": "npm run discover && npm run crawl",
    "prepare:trees": "tsx scripts/prepare-trees.ts",
    "build:data": "npm run prepare:trees && npm run parse && npm run normalize && npm run generate",
    "build:site": "vitepress build site",
    "build": "npm run fetch:data && npm run build:data && npm run build:site",
    "dev": "vitepress dev site",
    "cf:build": "npm run build:data && npm run build:site",
    "cf:publish-data": "tsx scripts/publish-r2.ts",
    "cf:deploy": "npx wrangler deploy"
  },
  "dependencies": {
    "cheerio": "^1.1.2"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@vitejs/plugin-vue": "^6.0.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitepress": "^2.0.0-alpha.12",
    "vitest": "^3.2.4",
    "vue": "^3.5.20"
  }
}
